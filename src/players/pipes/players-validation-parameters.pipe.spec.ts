import { PlayersValidationParametersPipe } from './players-validation-parameters.pipe';

describe('PlayersPipe', () => {
  it('should be defined', () => {
    expect(new PlayersValidationParametersPipe()).toBeDefined();
  });
});
